<UserControl
    x:Class="SpotifyWidget.Controls.SpotifyPlayerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SpotifyWidget.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContextChanged="OnDataContextChanged">

    <Grid Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Album Art + Track Info -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Width="80"
                Height="80"
                CornerRadius="8"
                Margin="0,0,12,0">
                <Image
                    Source="{x:Bind ViewModel.AlbumArt, Mode=OneWay}"
                    Stretch="UniformToFill"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock
                    Text="{x:Bind ViewModel.TrackName, Mode=OneWay}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    TextWrapping="NoWrap"
                    MaxLines="2" />
                <TextBlock
                    Text="{x:Bind ViewModel.ArtistName, Mode=OneWay}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    TextWrapping="NoWrap"
                    MaxLines="1"
                    Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <!-- Progress Bar -->
        <ProgressBar
            Grid.Row="1"
            Minimum="0"
            Maximum="{x:Bind ViewModel.ProgressMaximum, Mode=OneWay}"
            Value="{x:Bind ViewModel.ProgressValue, Mode=OneWay}"
            Height="4"
            Margin="0,0,0,4" />

        <!-- Time Display -->
        <TextBlock
            Grid.Row="2"
            Text="{x:Bind ViewModel.TimeDisplay, Mode=OneWay}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            HorizontalAlignment="Center"
            Margin="0,0,0,8" />

        <!-- Controls -->
        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Spacing="16">
            <Button
                Content="&#xE892;"
                Style="{StaticResource SymbolButtonStyle}"
                Command="{x:Bind ViewModel.PreviousTrackCommand}" />
            <Button
                Content="{x:Bind ViewModel.PlayPauseSymbol, Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}"
                Command="{x:Bind ViewModel.PlayPauseCommand}" />
            <Button
                Content="&#xE893;"
                Style="{StaticResource SymbolButtonStyle}"
                Command="{x:Bind ViewModel.NextTrackCommand}" />
        </StackPanel>
    </Grid>
</UserControl>